{
  "name": "network-weathermap",
  "version": "1.0.0",
  "description": "Network Weathermap with modern React Flow frontend",
  "private": true,
  "scripts": {
    "dev": "php -d extension=gd -S 0.0.0.0:8000 & npm --prefix ui run dev",
    "php:serve": "php -d extension=gd -S 0.0.0.0:8000",
    "ui:dev": "npm --prefix ui run dev",
    "ui:build": "npm --prefix ui run build"
  },
  "license": "MIT"
}
